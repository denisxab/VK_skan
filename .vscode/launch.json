{
  "configurations": [
    {
      "name": "Python: текущий файл",
      "type": "python",
      "request": "launch",
      // Путь к файлу который нуэно запустить
      "program": "${file}",
      // Где запускать консоль {integratedTerminal:КоносльVsCode,externalTerminal:ВнешнийТерминал}
      "console": "integratedTerminal",
      // Область отладки кода {true:ОтладкаТолькоМоегоКода,false:ОтладкаМоегоКодаИ_Библиотек}
      "justMyCode": false
      // Запустить прогрумм от root пользователя
     

      /*
			// Аргументы в интерпритатор
			"pythonArgs": ["args"],
			// Аргементы в командную строку при запуске
			"args": ["args_1"],
			// Атоматически перезапукать отладку при изменениях в коде
		   	"autoReload": {
				"enable": true
			 }
			 // Многопроцесорная отладка (особо не вникал как работает)
			 subProcess
			*/
    }
  ]
}
